[tools]
uv = "latest"

[env]
_.file = { path = ".env" }

[tasks.run_reader]
description = "RSSリーダーを実行"
run = "cd scripts && uv run python -m reader.main"

[tasks.test]
description = "テストを実行"
run = "uv run pytest tests/"
