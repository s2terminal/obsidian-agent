[tools]
node = "lts"
npm = "latest"
uv = "latest"

[env]
_.file = { path = ".env" }

[tasks.run_reader]
description = "RSSリーダーを実行"
run = "uv run python main.py reader"

[tasks.test]
description = "テストを実行"
run = "uv run pytest tests/"
