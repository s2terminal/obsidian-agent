services:
  app:
    build:
      context: .
      dockerfile: Containerfile
      target: development
    env_file:
      - .env
    environment:
      OBSIDIAN_ROOT: /vault
    volumes:
      - type: bind
        source: .
        target: /app
      - type: bind
        source: ${OBSIDIAN_ROOT}
        target: /vault
      - type: volume
        source: reader-cache
        target: /app/scripts/reader/.cache

volumes:
  reader-cache:
